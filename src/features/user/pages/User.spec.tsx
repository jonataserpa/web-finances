import { render } from '@testing-library/react'
import { ListUsers } from './ListUsers'
 

test('sum', () => {
    expect(1 + 1).toBe(2);
})
// describe('ListUsers', () => {
//     it ('should render a list of users', () => {
//         const { getByText } = render(<ListUsers />)

//         expect(getByText('Jonata serpa-editado')).toBeTruthy()
//     })
// })